function [ output_args ] = read_params( input_file, N )
%READ_PARAMS read input params for an MC simulation
%   input_file is the path of the file (full_input.txt) and N is the number
%   of simulations
fid = fopen(input_file);


end

